<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#BC92EC"
                android:centerColor="#BC92EC"
                android:centerY="1.0"
                android:endColor="#BC92EC"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#2185FF"
                    android:centerColor="#2185FF"
                    android:centerY="1.0"
                    android:endColor="#2185FF"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>